import { SearchUserFilterPipe } from './searchUser-filter.pipe';

describe('SearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
