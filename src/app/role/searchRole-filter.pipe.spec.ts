import { SearchRoleFilterPipe } from './searchRole-filter.pipe';

describe('SearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRoleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
