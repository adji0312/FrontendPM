import { SearchProjectTypeFilterPipe } from './searchProjectType-filter.pipe';

describe('SearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProjectTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
