import { SearchApplicationFilterPipe } from './searchApplication-filter.pipe';

describe('SearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchApplicationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
