import { SearchProjectFilterPipe } from './searchProject-filter.pipe';

describe('SearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProjectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
